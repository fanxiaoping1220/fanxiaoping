spring:
  application:
    name: langchain4j-2
  datasource:
    url: jdbc:mysql://rm-2ze4fy49tij35tigpfo.mysql.rds.aliyuncs.com:3306/test?useSSL=false&serverTimezone=UTC&characterEncoding=utf8&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: rendui,.!2026

server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

langchain4j:
  open-ai:
    chat-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: ${QWEN_API_KEY}
      model-name: qwen-plus
      log-responses: true
      log-requests: true
    streaming-chat-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: ${QWEN_API_KEY}
      model-name: qwen-plus
      log-responses: true
      log-requests: true
  community:
    dashscope:
      chat-model:
        api-key:  ${QWEN_API_KEY}
        model-name: qwen-plus
      streaming-chat-model:
        api-key: ${QWEN_API_KEY}
        model-name: qwen-plus
    redis:
      host: 1.94.101.207
      port: 6379
      password: PGFmy8pbBxeJYpYZ
      prefix: "langchain4j:arg:"
      user: default

# MyBatisPlus配置
# https://baomidou.com/config/
mybatis-plus:
  # 自定义配置 是否全局开启逻辑删除 关闭后 所有逻辑删除功能将失效
  enableLogicDelete: true
  # 多包名使用 例如 org.dromara.**.mapper,org.xxx.**.mapper
  mapperPackage: org.dromara.**.mapper
  # 对应的 XML 文件位置
  mapperLocations: classpath*:mapper/**/*Mapper.xml
  # 实体扫描，多个package用逗号或者分号分隔
  typeAliasesPackage: org.dromara.**.domain
  global-config:
    dbConfig:
      # 主键类型
      # AUTO 自增 NONE 空 INPUT 用户输入 ASSIGN_ID 雪花 ASSIGN_UUID 唯一 UUID
      # 如需改为自增 需要将数据库表全部设置为自增
      idType: ASSIGN_ID
  configuration:
    # 开启SQL日志
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl


logging:
  level:
    dev.langchain4j: debug
    root: info
    # MyBatis SQL日志
    com.baomidou.mybatisplus: debug
    com.baomidou.mybatisplus.mapper: trace
    # MyBatis日志
    org.mybatis: debug
    # JDBC SQL执行日志
    org.springframework.jdbc.core.JdbcTemplate: debug
    org.springframework.jdbc.core.StatementCreatorUtils: trace
    # SQL日志
    com.mysql.cj.jdbc: DEBUG
    com.mysql.cj.protocol: DEBUG